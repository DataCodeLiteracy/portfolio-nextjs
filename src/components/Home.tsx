import styles from '../app/page.module.css'

const Home = () => {
  return (
    <section id="#home" className={`${styles.section} ${styles.home}`}>
      <h1>Home 섹션</h1>
    </section>
  )
}

export default Home
