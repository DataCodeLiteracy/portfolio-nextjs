'use client'
import React, { useRef, useState, useEffect } from 'react'

const ChangeImage = () => {
  return {
    handleMouseEnter,
    handleMouseLeave,
    handleClick,
    imageRef
  }
}

export default ChangeImage
